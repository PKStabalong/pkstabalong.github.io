{
  "devDependencies": {
    "tailwindcss": "^3.1.8"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tailwind":"npx tailwindcss -i ./public/input.css -o ./src/tailwind.css --watch"
  }
}
